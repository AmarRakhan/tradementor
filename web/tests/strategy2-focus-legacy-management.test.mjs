import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const maker=fs.readFileSync(new URL("../components/aster-strategy2-maker.tsx",import.meta.url),"utf8");test("legacy Focus management is absent from active maker",()=>{assert.doesNotMatch(maker,/Focus|focusLive|focusV2/);assert.match(maker,/multi_bb_v1/);});
