import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const maker=fs.readFileSync(new URL("../components/aster-strategy2-maker.tsx",import.meta.url),"utf8");test("Multi BB has no hedge or re-hedge wizard",()=>{assert.doesNotMatch(maker,/hedge|re-hedge/i);assert.match(maker,/Geen hedge\/airbag\/portfolio-TP\/Focus-state machine/);});
