import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const maker=fs.readFileSync(new URL("../components/aster-strategy2-maker.tsx",import.meta.url),"utf8");test("Multi BB entry sizing is fixed notional with explicit DCA margin",()=>{assert.match(maker,/Entry orderwaarde \(USDT\)/);assert.match(maker,/DCA margin \(USDT\)/);assert.doesNotMatch(maker,/focusExposurePreview/);});
