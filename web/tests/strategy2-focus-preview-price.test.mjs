import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const maker=fs.readFileSync(new URL("../components/aster-strategy2-maker.tsx",import.meta.url),"utf8");test("Multi BB sizing is explicit margin rather than Focus preview math",()=>{assert.match(maker,/Entry margin \(USDT\)/);assert.match(maker,/DCA margin \(USDT\)/);assert.doesNotMatch(maker,/focusExposurePreview/);});
