import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const maker=fs.readFileSync(new URL("../components/aster-strategy2-maker.tsx",import.meta.url),"utf8");test("Multi BB live start remains explicit and readiness-gated",()=>{assert.match(maker,/async function toggleLive/);assert.match(maker,/if\(status\.pending\)return/);assert.match(maker,/if\(liveReady\)\{await action\("start"\)/);assert.match(maker,/await checkReadiness\(\)/);});
