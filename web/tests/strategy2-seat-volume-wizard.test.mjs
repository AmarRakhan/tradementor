import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const maker=fs.readFileSync(new URL("../components/aster-strategy2-maker.tsx",import.meta.url),"utf8");test("wizard persists user Top-N and free LONG SHORT seat targets",()=>{assert.match(maker,/universeTopN:Math\.round/);assert.match(maker,/maximumPositions:Math\.round/);assert.match(maker,/longSlots:Math\.round/);assert.match(maker,/shortSlots:Math\.round/);assert.match(maker,/Top-N volume/);});
