import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const maker=fs.readFileSync(new URL("../components/aster-strategy2-maker.tsx",import.meta.url),"utf8");test("Money Grabber and portfolio protection are retired from active strategy UI",()=>{assert.doesNotMatch(maker,/Money Grabber|Portfolio Protection/);assert.match(maker,/Multi DCA/);});test("new strategy uses one shared bounded DCA configuration",()=>{assert.match(maker,/DCA afstand \(%\)/);assert.match(maker,/DCA margin \(USDT\)/);assert.match(maker,/Globale DCA-limiet \(0–3\)/);});
