import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const maker=fs.readFileSync(new URL("../components/aster-strategy2-maker.tsx",import.meta.url),"utf8");test("Money Grabber and portfolio protection are retired from active strategy UI",()=>{assert.doesNotMatch(maker,/Money Grabber|Portfolio Protection/);assert.match(maker,/Multi BB DCA/);});test("new strategy uses one mirrored DCA control for LONG and SHORT",()=>{assert.match(maker,/LONG koopt lager bij; SHORT koopt hoger bij/);assert.match(maker,/Max automatische DCA's/);});
