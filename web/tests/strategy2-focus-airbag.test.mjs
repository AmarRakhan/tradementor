import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const maker=fs.readFileSync(new URL("../components/aster-strategy2-maker.tsx",import.meta.url),"utf8");test("legacy Focus Airbag is removed from active strategy maker",()=>{assert.doesNotMatch(maker,/focusAirbag|PORTFOLIO AIRBAG|Hedge bijdrage/);assert.match(maker,/Globale DCA-limiet \(0–\$\{MAX_DCA\}\)/);});
