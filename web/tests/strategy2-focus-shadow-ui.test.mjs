import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const maker=fs.readFileSync(new URL("../components/aster-strategy2-maker.tsx",import.meta.url),"utf8");test("Focus Shadow is retired from the new strategy UI",()=>{assert.doesNotMatch(maker,/Focus Shadow|focusShadow/);assert.match(maker,/Veilig simuleren/);});
