import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const maker=fs.readFileSync(new URL("../components/aster-strategy2-maker.tsx",import.meta.url),"utf8");test("direct Multi BB settings expose per-position TP",()=>{assert.match(maker,/Take Profit \(%\)/);assert.match(maker,/takeProfit: n\(v\.tp\) \/ 100/);assert.doesNotMatch(maker,/portfolio-cyclus|Winsttrigger/);});
