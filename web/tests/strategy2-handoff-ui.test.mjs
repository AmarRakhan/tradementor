import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const maker=fs.readFileSync(new URL("../components/aster-strategy2-maker.tsx",import.meta.url),"utf8");test("old handoff UI is retired; exchange truth remains explicit",()=>{assert.doesNotMatch(maker,/Exclusieve overdracht voltooien/);assert.match(maker,/Exchange truth/);assert.match(maker,/handmatige toevoegingen/);});
