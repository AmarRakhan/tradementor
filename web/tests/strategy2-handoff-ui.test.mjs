import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const maker=fs.readFileSync(new URL("../components/aster-strategy2-maker.tsx",import.meta.url),"utf8");test("old handoff UI is retired; server truth remains explicit",()=>{assert.doesNotMatch(maker,/Exclusieve overdracht voltooien/);assert.match(maker,/door de server bevestigde configuratie is leidend/);assert.match(maker,/server-side opgeslagen en bevestigd/);});
