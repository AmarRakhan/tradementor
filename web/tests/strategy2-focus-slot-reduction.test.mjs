import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const maker=fs.readFileSync(new URL("../components/aster-strategy2-maker.tsx",import.meta.url),"utf8");test("slot distribution is direct and bounded",()=>{assert.match(maker,/Math\.min\(50, longSlots \+ shortSlots\)/);assert.match(maker,/setLong/);assert.match(maker,/setShort/);assert.match(maker,/maximaal 25 LONG \+ 25 SHORT \(50 totaal\)/);assert.doesNotMatch(maker,/Multi-Focus/);});
