import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const maker=fs.readFileSync(new URL("../components/aster-strategy2-maker.tsx",import.meta.url),"utf8");test("direct settings expose Multi BB controls only",()=>{for(const x of ["Top-N volume","Minimum leverage","Totaal posities","DCA margin","Take Profit","CROSS"])assert.match(maker,new RegExp(x));assert.match(maker,/Geen wizard/);assert.doesNotMatch(maker,/Focus 2\.0/);});
