import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const maker=fs.readFileSync(new URL("../components/aster-strategy2-maker.tsx",import.meta.url),"utf8");test("strategy wizard exposes Multi BB controls only",()=>{for(const x of ["Top-N Aster-volume","Minimum leverage","Direct slots vullen","DCA-bedrag en harde limiet","Take profit","Cross margin"])assert.match(maker,new RegExp(x));assert.doesNotMatch(maker,/Focus 2\.0/);});
