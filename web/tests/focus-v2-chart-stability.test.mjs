import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const maker=fs.readFileSync(new URL("../components/aster-strategy2-maker.tsx",import.meta.url),"utf8");test("retired Focus chart controls are absent from the active strategy maker",()=>{for(const x of ["Focus 2.0 gebruiken","SHORT RELEASE","PORTFOLIO AIRBAG"])assert.doesNotMatch(maker,new RegExp(x));assert.match(maker,/1-minuut Bollinger-entry/);});
