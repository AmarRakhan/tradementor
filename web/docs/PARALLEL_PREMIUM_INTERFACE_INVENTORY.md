# Parallel premium interface inventory

Existing trusted interface remains the default.

The premium interface is a presentation-only parallel view. Switching views may persist a preference, but cannot start or stop a strategy, close a position, change ownership, or submit an order. Both views reuse the same authenticated, user-scoped cloud data and retain the trusted view as the rollback path.
