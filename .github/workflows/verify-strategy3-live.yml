name: Retired - Strategy 3 live verification

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Strategy 3 is retired
        run: echo "Strategy 3 live verification is retired; no scheduler, service, or cloud project is queried."
