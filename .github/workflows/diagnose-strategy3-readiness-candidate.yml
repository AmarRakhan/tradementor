name: Retired - Strategy 3 candidate diagnostics

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Strategy 3 is retired
        run: echo "Strategy 3 candidate diagnostics are retired; no image or cloud project is accessed."
