name: Retired - Strategy 3 live promotion

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Strategy 3 is retired
        run: echo "Strategy 3 live promotion is retired; no deployment or traffic change is permitted."
