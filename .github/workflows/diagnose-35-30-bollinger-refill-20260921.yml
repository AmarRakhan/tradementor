name: Read-only diagnose 35-30 refill subtype 20260921

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  issues: write

concurrency:
  group: tradementor-readonly-35-30-bollinger-diagnosis
  cancel-in-progress: false

env:
  GCP_PROJECT_ID: tradementor-production
  GCP_REGION: europe-west4
  CLOUD_RUN_SERVICE: tradementor-api
  WIF_PROVIDER: projects/604335232956/locations/global/workloadIdentityPools/github/providers/tradementor-production
  DIAG_SERVICE_ACCOUNT: github-tradementor-deploy@tradementor-production.iam.gserviceaccount.com
  RESULT_ISSUE: '209'

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.WIF_PROVIDER }}
          service_account: ${{ env.DIAG_SERVICE_ACCOUNT }}
      - uses: google-github-actions/setup-gcloud@v2

      - name: Distinguish minimum-order from available-margin block
        shell: bash
        run: |
          set -euo pipefail
          gcloud run services describe "$CLOUD_RUN_SERVICE" --project "$GCP_PROJECT_ID" --region "$GCP_REGION" --format=json > "$RUNNER_TEMP/service.json"
          readarray -t VALUES < <(python - "$RUNNER_TEMP/service.json" <<'PY'
          import json,sys
          s=json.load(open(sys.argv[1],encoding='utf-8'))
          spec=s['spec']['template']['spec']
          print(spec['containers'][0]['image'])
          print(spec['serviceAccountName'])
          PY
          )
          IMAGE="${VALUES[0]}"; RUNTIME_SA="${VALUES[1]}"
          : > "$RUNNER_TEMP/flags.txt"

          SCRIPT_B64="$(base64 -w0 <<'PY'
          import os
          from google.cloud import firestore
          project='tradementor-production'
          test=os.environ.get('DIAG_TEST','')
          db=firestore.Client(project=project)

          def num(v,default=0):
              try:return int(round(float(v)))
              except:return default

          matches=[]
          for snap in db.collection('asterStrategy2').stream():
              raw=snap.to_dict() or {}
              s=raw.get('settings') if isinstance(raw.get('settings'),dict) else {}
              if num(s.get('longSlots'),-1)!=35: continue
              if num(s.get('shortSlots'),-1)!=30: continue
              if num(s.get('universeTopN'),-1)!=350: continue
              if num(s.get('maximumPositions'),-1)!=65: continue
              if num(s.get('minimumLeverage'),-1)!=20: continue
              matches.append(raw)

          unique=len(matches)==1
          raw=matches[0] if unique else {}
          s=raw.get('settings') if isinstance(raw.get('settings'),dict) else {}
          r=raw.get('multiBbReport') if isinstance(raw.get('multiBbReport'),dict) else {}
          reasons=r.get('entrySkipReasons') if isinstance(r.get('entrySkipReasons'),dict) else {}
          reason_text=' '.join(str(k).upper() for k in reasons)
          truth=False
          if test=='unique': truth=unique
          elif test=='minimum_order': truth=unique and num(r.get('minimumOrderRejectedCount'))>0
          elif test=='budget_wait': truth=unique and str(r.get('entryStatus') or '')=='WAITING_BUDGET'
          elif test=='fixed_notional': truth=unique and str(s.get('entrySizingMode') or '').lower()=='notional'
          elif test=='margin_reason': truth=unique and ('MARGIN' in reason_text or 'BUDGET' in reason_text)
          raise SystemExit(0 if truth else 9)
          PY
          )"

          run_test() {
            local test="$1"; local suffix="$2"; local job="tm3530s-${GITHUB_RUN_ID: -5}-$suffix"
            gcloud run jobs deploy "$job" --project "$GCP_PROJECT_ID" --region "$GCP_REGION"               --image "$IMAGE" --service-account "$RUNTIME_SA" --command python               --args=-c,"import base64;exec(base64.b64decode('$SCRIPT_B64'))"               --set-env-vars "DIAG_TEST=$test" --tasks 1 --parallelism 1 --max-retries 0 --task-timeout 120s --quiet >/dev/null
            set +e
            gcloud run jobs execute "$job" --project "$GCP_PROJECT_ID" --region "$GCP_REGION" --wait >/dev/null 2>&1
            local rc=$?
            set -e
            if [ "$rc" -eq 0 ]; then echo "$test=1" >> "$RUNNER_TEMP/flags.txt"; else echo "$test=0" >> "$RUNNER_TEMP/flags.txt"; fi
            gcloud run jobs delete "$job" --project "$GCP_PROJECT_ID" --region "$GCP_REGION" --quiet >/dev/null 2>&1 || true
          }

          run_test unique u
          run_test minimum_order m
          run_test budget_wait b
          run_test fixed_notional n
          run_test margin_reason r

      - name: Publish subtype result
        if: always()
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python - "$RUNNER_TEMP/flags.txt" > "$RUNNER_TEMP/result.md" <<'PY'
          import sys
          flags={}
          try:
              for line in open(sys.argv[1],encoding='utf-8'):
                  if '=' in line:
                      k,v=line.strip().split('=',1); flags[k]=v=='1'
          except Exception: pass
          print('## Refill-block vervolgdiagnose')
          print()
          if not flags.get('unique'):
              print('- Result: **REFUSED_NOT_UNIQUE_OR_READ_FAILED**')
          else:
              print(f'- Vaste positieomvang / notional mode: **{"AAN" if flags.get("fixed_notional") else "UIT"}**')
              print(f'- Minimum-exchangeorder rejects aanwezig: **{"JA" if flags.get("minimum_order") else "NEE"}**')
              print(f'- Beschikbare-margin wait actief: **{"JA" if flags.get("budget_wait") else "NEE"}**')
              print(f'- Overige margin/budget rejectcode aanwezig: **{"JA" if flags.get("margin_reason") else "NEE"}**')
              print()
              print('### Conclusie')
              if flags.get('minimum_order') and not flags.get('budget_wait'):
                  print('De blokkade komt van de minimale exchange-order / te kleine geplande instap, niet van te weinig beschikbare accountmargin en niet van Bollinger.')
              elif flags.get('budget_wait'):
                  print('De blokkade is daadwerkelijk onvoldoende beschikbare margin voor de geplande nieuwe instap.')
              elif flags.get('margin_reason'):
                  print('De blokkade zit in een margin/notional-validatie tijdens entry planning.')
              else:
                  print('De eerdere margin/minimum-order classificatie is niet meer actief; de scannerstatus is intussen gewijzigd.')
          print()
          print('_Read-only; geen instellingen of orders gewijzigd._')
          PY
          gh issue comment "$RESULT_ISSUE" --repo "$GITHUB_REPOSITORY" --body-file "$RUNNER_TEMP/result.md"
