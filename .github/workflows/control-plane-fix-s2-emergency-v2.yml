name: Control Plane Fix Strategy 2 Emergency V2

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Superseded: the app-wide Strategy 2 emergency-entry fix is now committed directly on amar-crypto-bot-2026-cloud.'
          echo 'Do not use this workflow to rewrite application source.'
