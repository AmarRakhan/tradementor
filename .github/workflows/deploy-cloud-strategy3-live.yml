name: Retired - Strategy 3 live runtime

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Strategy 3 is retired
        run: |
          echo "Strategy 3 is retired. No deployment, scheduler, cloud project, or live execution is permitted."
