name: Focus Live Backend Deploy
on:
  push:
    branches: [amar-crypto-bot-2026-cloud]
    paths: [.focus-live-backend-deploy-trigger]
permissions:
  contents: write
  id-token: write
concurrency:
  group: focus-live-production-backend
  cancel-in-progress: false
env:
  GCP_PROJECT_ID: tradementor-production
  GCP_REGION: europe-west4
  ARTIFACT_REPOSITORY: tradementor
  CLOUD_RUN_SERVICE: tradementor-api
  WIF_PROVIDER: projects/604335232956/locations/global/workloadIdentityPools/github/providers/tradementor-production
  DEPLOY_SERVICE_ACCOUNT: github-tradementor-deploy@tradementor-production.iam.gserviceaccount.com
jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: amar-crypto-bot-2026-cloud
          fetch-depth: 0
      - name: Verify source and safe activation contract
        run: |
          test "$GITHUB_REPOSITORY" = "AmarRakhan/tradementor"
          test "$GITHUB_REF" = "refs/heads/amar-crypto-bot-2026-cloud"
          grep -q 'focus_live_enabled: bool = False' cloud_api/aster_strategy2.py
          grep -q 'settings=replace(settings,focus_live_enabled=bool(settings.trading_mode=="focus" and settings.mode=="live"))' cloud_api/main.py
          grep -q 'if settings.trading_mode!="focus" or not settings.focus_live_enabled:return None' cloud_api/aster_strategy2_focus_live.py
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: cloud_api/requirements.txt
      - name: Run complete backend suite
        run: |
          pip install -r cloud_api/requirements.txt pytest
          cd cloud_api
          pytest -q
      - uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.WIF_PROVIDER }}
          service_account: ${{ env.DEPLOY_SERVICE_ACCOUNT }}
      - uses: google-github-actions/setup-gcloud@v2
      - name: Capture production configuration and IAM
        run: |
          gcloud run services describe "$CLOUD_RUN_SERVICE" --project "$GCP_PROJECT_ID" --region "$GCP_REGION" --format=json > "$RUNNER_TEMP/before.json"
          gcloud run services get-iam-policy "$CLOUD_RUN_SERVICE" --project "$GCP_PROJECT_ID" --region "$GCP_REGION" --format=json > "$RUNNER_TEMP/iam-before.json"
          python - "$RUNNER_TEMP/before.json" >> "$GITHUB_ENV" <<'PY'
          import json,sys
          service=json.load(open(sys.argv[1]))
          spec=service["spec"]["template"]["spec"]
          env={x["name"]:x.get("value") for x in spec["containers"][0].get("env",[])}
          assert env.get("ASTER_LIVE_EXECUTION_ENABLED")=="true"
          assert env.get("ASTER_STRATEGY2_LIVE_ENABLED")=="true"
          live=[x for x in service["status"].get("traffic",[]) if int(x.get("percent",0))==100]
          assert len(live)==1
          print(f'BEFORE_REVISION={live[0]["revisionName"]}')
          print(f'SERVICE_URL={service["status"]["url"]}')
          PY
      - name: Build and push exact backend
        run: |
          gcloud auth configure-docker "$GCP_REGION-docker.pkg.dev" --quiet
          IMAGE="$GCP_REGION-docker.pkg.dev/$GCP_PROJECT_ID/$ARTIFACT_REPOSITORY/tradementor-api:$GITHUB_SHA"
          docker build --tag "$IMAGE" cloud_api
          docker push "$IMAGE"
          echo "IMAGE=$IMAGE" >> "$GITHUB_ENV"
      - name: Deploy candidate with zero traffic
        run: |
          TAG="focus-live-${GITHUB_RUN_NUMBER}-${GITHUB_RUN_ATTEMPT}"
          SUFFIX="fl-${GITHUB_SHA:0:8}-${GITHUB_RUN_ATTEMPT}"
          gcloud run services update "$CLOUD_RUN_SERVICE" --project "$GCP_PROJECT_ID" --region "$GCP_REGION" --image "$IMAGE" --revision-suffix "$SUFFIX" --no-traffic --tag "$TAG" --quiet
          gcloud run services describe "$CLOUD_RUN_SERVICE" --project "$GCP_PROJECT_ID" --region "$GCP_REGION" --format=json > "$RUNNER_TEMP/candidate.json"
          python - "$RUNNER_TEMP/before.json" "$RUNNER_TEMP/candidate.json" "$TAG" >> "$GITHUB_ENV" <<'PY'
          import json,sys
          before=json.load(open(sys.argv[1]));after=json.load(open(sys.argv[2]));tag=sys.argv[3]
          b=before["spec"]["template"]["spec"];a=after["spec"]["template"]["spec"]
          assert b.get("serviceAccountName")==a.get("serviceAccountName")
          assert b.get("timeoutSeconds")==a.get("timeoutSeconds")
          assert b.get("containerConcurrency")==a.get("containerConcurrency")
          assert b["containers"][0].get("env",[])==a["containers"][0].get("env",[])
          row=next(x for x in after["status"]["traffic"] if x.get("tag")==tag)
          print(f'CANDIDATE_REVISION={row["revisionName"]}')
          print(f'CANDIDATE_URL={row["url"]}')
          PY
      - name: Verify candidate before traffic
        run: |
          HEALTH="$(curl --fail --silent --show-error --retry 6 --retry-delay 3 "$CANDIDATE_URL/health")"
          python - "$HEALTH" <<'PY'
          import json,sys
          h=json.loads(sys.argv[1]);assert h["status"]=="ready";assert h["multiUser"] is True
          PY
          for path in /v1/me/aster/strategy2/focus/markets /v1/me/aster/strategy2/focus/shadow; do
            code="$(curl -s -o /dev/null -w '%{http_code}' "$CANDIDATE_URL$path")"
            test "$code" = "401" -o "$code" = "403"
          done
      - name: Promote verified candidate
        run: |
          gcloud run services update-traffic "$CLOUD_RUN_SERVICE" --project "$GCP_PROJECT_ID" --region "$GCP_REGION" --to-revisions "$CANDIDATE_REVISION=100" --quiet
          HEALTH="$(curl --fail --silent --show-error --retry 6 --retry-delay 3 "$SERVICE_URL/health")"
          python - "$HEALTH" <<'PY'
          import json,sys
          h=json.loads(sys.argv[1]);assert h["status"]=="ready";assert h["multiUser"] is True
          PY
          gcloud run services describe "$CLOUD_RUN_SERVICE" --project "$GCP_PROJECT_ID" --region "$GCP_REGION" --format=json > "$RUNNER_TEMP/after.json"
          python - "$RUNNER_TEMP/after.json" "$CANDIDATE_REVISION" <<'PY'
          import json,sys
          service=json.load(open(sys.argv[1]));candidate=sys.argv[2]
          live=[x for x in service["status"].get("traffic",[]) if int(x.get("percent",0))==100]
          assert len(live)==1 and live[0].get("revisionName")==candidate,live
          PY
      - name: Verify IAM unchanged and cleanup one-shot files
        run: |
          gcloud run services get-iam-policy "$CLOUD_RUN_SERVICE" --project "$GCP_PROJECT_ID" --region "$GCP_REGION" --format=json > "$RUNNER_TEMP/iam-after.json"
          python - "$RUNNER_TEMP/iam-before.json" "$RUNNER_TEMP/iam-after.json" <<'PY'
          import json,sys
          assert json.load(open(sys.argv[1]))==json.load(open(sys.argv[2]))
          PY
          rm -f .github/workflows/focus-live-backend-deploy.yml .focus-live-backend-deploy-trigger
          git config user.name OpenAI
          git config user.email noreply@openai.com
          git add -A
          git commit -m "chore: remove Focus live backend one-shot deploy"
          git push origin HEAD:amar-crypto-bot-2026-cloud
